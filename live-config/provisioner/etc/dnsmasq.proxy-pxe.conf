# ProxyDHCP PXE only. Router 192.168.1.100 hands out leases.
port=0
interface=enp0s25
bind-interfaces

# Proxy on subnet
dhcp-range=192.168.1.0,proxy,255.255.255.0

# Detect UEFI x86_64 (ARCH=7)
dhcp-match=set:efi64,option:client-arch,7

# Offer PXE services (correct syntax includes the "type")
pxe-service=tag:efi64,x86-64_EFI,"UEFI iPXE",ipxe-intel.efi
pxe-service=x86PC,"BIOS iPXE",ipxe-intel.pxe

# Ensure next-server is set in the BOOTP header for compatibility
dhcp-boot=tag:efi64,ipxe-intel.efi,,192.168.1.44
dhcp-boot=ipxe-intel.pxe,,192.168.1.44

log-dhcp
