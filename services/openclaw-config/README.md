# OpenClaw Config

This tree holds tracked OpenClaw agents, prompts, provider templates, bindings, and policy for Glasslab v2.

Export this directory into the cluster with `./scripts/export-openclaw-config.sh` before scaling the OpenClaw deployment.
