{
  "id": "workflow-api-tool",
  "name": "Workflow API Tool",
  "description": "Narrow workflow-api helper for Glasslab v2 validation.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "minLength": 1,
        "description": "Approved workflow-api base URL for internal Glasslab v2 reads and bounded run submission."
      },
      "validationRunRequest": {
        "type": "object",
        "additionalProperties": true,
        "description": "Repo-managed bounded POST /runs payload used for first operator validation."
      },
      "knownWorkflowIds": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        },
        "minItems": 1,
        "description": "Approved workflow_ids exposed to the tiny experimental lookup tool."
      },
      "timeoutSeconds": {
        "type": "number",
        "minimum": 1,
        "maximum": 30,
        "default": 10
      }
    },
    "required": ["baseUrl", "validationRunRequest", "knownWorkflowIds"]
  }
}
