{
  "id": "workflow-api-tool",
  "name": "Workflow API Tool",
  "description": "Read-only workflow-api helper for Glasslab v2 operator validation.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "minLength": 1,
        "description": "Approved workflow-api base URL for internal Glasslab v2 reads."
      },
      "timeoutSeconds": {
        "type": "number",
        "minimum": 1,
        "maximum": 30,
        "default": 10
      }
    },
    "required": ["baseUrl"]
  }
}
