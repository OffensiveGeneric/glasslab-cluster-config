apiVersion: v1
kind: Service
metadata:
  name: glasslab-research-ingress
  namespace: glasslab-v2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: glasslab-research-ingress
  ports:
    - name: http
      port: 8096
      targetPort: http
