# Assessment Agent

ClusterIP deployment manifests for the bounded `assessment-agent` service live here.

Current intended role:

- assess replicability and execution readiness behind `workflow-api`
- remain internal-only in `glasslab-v2`
- stay feature-flagged off in `workflow-api` until explicitly enabled
