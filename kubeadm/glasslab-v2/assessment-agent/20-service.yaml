apiVersion: v1
kind: Service
metadata:
  name: glasslab-assessment-agent
  namespace: glasslab-v2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: glasslab-assessment-agent
  ports:
    - name: http
      port: 8092
      targetPort: http
