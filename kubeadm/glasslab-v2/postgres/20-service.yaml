apiVersion: v1
kind: Service
metadata:
  name: glasslab-postgres
  namespace: glasslab-v2
spec:
  selector:
    app.kubernetes.io/name: glasslab-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
