# Design Agent

ClusterIP deployment manifests for the bounded `design-agent` service live here.

Current intended role:

- produce reviewable design drafts behind `workflow-api`
- remain internal-only in `glasslab-v2`
- stay feature-flagged off in `workflow-api` until explicitly enabled
