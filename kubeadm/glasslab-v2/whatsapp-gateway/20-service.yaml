apiVersion: v1
kind: Service
metadata:
  name: glasslab-whatsapp-gateway
  namespace: glasslab-v2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: glasslab-whatsapp-gateway
  ports:
    - name: http
      port: 8097
      targetPort: http
