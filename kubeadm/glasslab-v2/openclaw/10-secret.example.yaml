apiVersion: v1
kind: Secret
metadata:
  name: glasslab-openclaw
  namespace: glasslab-v2
type: Opaque
stringData:
  OPENCLAW_GATEWAY_TOKEN: change-me-before-deploy
  OPENCLAW_VLLM_API_KEY: change-me-before-deploy
  # Optional: required only when enabling the WhatsApp validation channel.
  OPENCLAW_WHATSAPP_OWNER: +15551234567
