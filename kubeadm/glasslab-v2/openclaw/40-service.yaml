apiVersion: v1
kind: Service
metadata:
  name: glasslab-openclaw
  namespace: glasslab-v2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: glasslab-openclaw
  ports:
    - name: http
      port: 18789
      targetPort: http
