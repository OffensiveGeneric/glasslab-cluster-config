apiVersion: v1
kind: Service
metadata:
  name: glasslab-gpu-runner
  namespace: glasslab-v2
spec:
  selector:
    app.kubernetes.io/name: glasslab-gpu-runner
  ports:
    - protocol: TCP
      port: 52415
      targetPort: http
  type: ClusterIP
