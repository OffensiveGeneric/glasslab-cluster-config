apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: runner-model-cache
  namespace: glasslab-v2
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-nfs-storage
  resources:
    requests:
      storage: 50Gi
