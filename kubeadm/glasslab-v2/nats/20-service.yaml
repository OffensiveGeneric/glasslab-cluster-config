apiVersion: v1
kind: Service
metadata:
  name: glasslab-nats
  namespace: glasslab-v2
spec:
  selector:
    app.kubernetes.io/name: glasslab-nats
  ports:
    - name: client
      port: 4222
      targetPort: client
    - name: monitoring
      port: 8222
      targetPort: monitoring
