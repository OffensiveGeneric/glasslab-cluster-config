apiVersion: v1
kind: Service
metadata:
  name: glasslab-minio
  namespace: glasslab-v2
spec:
  selector:
    app.kubernetes.io/name: glasslab-minio
  ports:
    - name: api
      port: 9000
      targetPort: api
    - name: console
      port: 9001
      targetPort: console
