apiVersion: v1
kind: Service
metadata:
  name: glasslab-intake-agent
  namespace: glasslab-v2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: glasslab-intake-agent
  ports:
    - name: http
      port: 8090
      targetPort: http
