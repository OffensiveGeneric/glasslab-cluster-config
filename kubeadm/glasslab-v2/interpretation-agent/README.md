# Interpretation Agent

ClusterIP deployment manifests for the bounded `interpretation-agent` service live here.

Current intended role:

- interpret the latest intake record behind `workflow-api`
- remain internal-only in `glasslab-v2`
- stay feature-flagged off in `workflow-api` until explicitly enabled
